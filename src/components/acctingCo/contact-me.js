import React from "react"

export default props => (
  <section className="contactMecontainer" key={props.key}>
    <div>
      <h2 className="title">Contact Us</h2>
    </div>
    <div className="text">
      <p>
        If you are interested in working together or wish to find out more about
        what We can help with, use one of the below methods to get in touch with
        me.
      </p>
    </div>
  </section>
)
